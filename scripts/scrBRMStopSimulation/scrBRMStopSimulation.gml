// clear whats left of the vehicle

instance_destroy(oPartRoot);

// serialise in the current state if one exists

// if current json file exists load it in and instantiate

if(file_exists(filenameCurrent))
{
}
